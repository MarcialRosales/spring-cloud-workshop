spring:
  application:
    name: eureka-server
  cloud.config.uri: http://localhost:8888

# move this to central config
eureka:
  client: #  switch off the client side behaviour, so it doesn’t keep trying and failing to reach its peers
    registerWithEureka: false 
    fetchRegistry: false 

  